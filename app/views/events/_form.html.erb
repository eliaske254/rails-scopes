<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :clocked_in, style: "display: block" %>
    <%= form.datetime_field :clocked_in %>
  </div>

  <div>
    <%= form.label :clocked_out, style: "display: block" %>
    <%= form.datetime_field :clocked_out %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
